import React, {Component} from 'react';
import DisplayCarousel from '../DisplayComponents/DisplayCarousel';
import {
  Link
} from 'react-router-dom';

class Characters extends Component {
    render() {
      return (
          <DisplayCarousel/>
      );
    }
}
  
  export default Characters;